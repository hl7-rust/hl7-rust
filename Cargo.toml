[package]
name = "hl7-v2-from-er7-into-json"
version = "0.4.0"
authors = ["Joel Parker Henderson <joel@joelparkerhenderson.com>"]
edition = "2024"
description = "HL7 v2 from ER7 into JSON: Convert Health Level Seven (HL7) version 2 (v2) messages from Encoding Rules version 7 (ER7) a.k.a. pipe-delimited into JavaScript Object Notation (JSON)."
license = "MIT OR Apache-2.0 OR BSD-3-Clause OR GPL-2.0-only OR GPL-3.0-only"
repository = "https://github.com/hl7-rust/hl7-v2-from-er7-into-json"
keywords = ["hl7", "healthcare", "json", "er7", "v2"]
categories = ["parser-implementations", "command-line-utilities"]

[dependencies]
# The ER7 encoding layer: parsing, escape sequences, and writing back out.
# This crate supplies only the HL7 v2.5 dictionary on top of it — see
# spec/index.md §2.
er7 = "0.1.0"
