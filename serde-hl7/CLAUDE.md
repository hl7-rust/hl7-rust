# CLAUDE.md

See [AGENTS.md](AGENTS.md) — that file holds the canonical agent
instructions for this crate. Keeping the guidance in one file avoids
the two drifting apart.

The behavior itself is specified in `serde-hl7-v2`'s
[spec/index.md](../serde-hl7-v2/spec/index.md) (single source of truth
for `serde_hl7::v2`) and `serde-hl7-v3`'s
[spec/index.md](../serde-hl7-v3/spec/index.md) (single source of truth
for `serde_hl7::v3`); this crate has no spec of its own, and
[README.md](README.md) is the user-facing summary.
