[package]
name = "hl7-2-from-er7-into-json"
version = "0.5.0"
authors = ["Joel Parker Henderson <joel@joelparkerhenderson.com>"]
edition = "2024"
rust-version.workspace = true
description = "HL7® v2 from ER7 into JSON: Convert Health Level Seven (HL7) version 2 (v2) messages from Encoding Rules version 7 (ER7) a.k.a. pipe-delimited into JavaScript Object Notation (JSON). HL7®, and FHIR® are the registered trademarks of Health Level Seven International and their use of these trademarks does not constitute an endorsement by HL7. This project is an independent work."
license = "MIT OR Apache-2.0 OR BSD-3-Clause OR GPL-2.0-only OR GPL-3.0-only"
repository = "https://github.com/hl7-rust/hl7-rust"
keywords = ["hl7", "healthcare", "json", "er7", "v2"]
categories = ["parser-implementations", "command-line-utilities"]

[dependencies]
# The ER7 encoding layer: parsing, escape sequences, and writing back out.
# This crate supplies only the HL7 v2.5 dictionary on top of it — see
# spec/index.md §2.
er7 = "0.1.0"

# Criterion is a development dependency only: it is compiled for
# `cargo bench` and never linked into the library or the binary, so the
# runtime dependency list above is unchanged.
[dev-dependencies]
criterion = { version = "0.8", default-features = false, features = ["cargo_bench_support"] }

[[bench]]
name = "convert"
harness = false
